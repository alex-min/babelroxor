#ifndef UTILS_MATH_H_
#define UTILS_MATH_H_

#include <cstdlib>
#include <time.h>
#include <math.h>

#include "Config.h"

namespace Utils
{
    namespace Math
    {
        //inline void LINK_OPTION_UTILS   InitRand()                          {srand(time(0));}
        //template<typename T> T          Rand(const T &min, const T &max)    {return min + ((T)rand() / ((float)RAND_MAX + 1) * max);}

        //template <typename T>
//        void Swap(T &v1, T &v2)
//        {
//            T tmp = v2;
//            v2 = v1;
//            v1 = tmp;
//        }
    }
}

#endif
